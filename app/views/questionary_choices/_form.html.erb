

<%= form_for(questionary_choice) do |f| %>
  <% if questionary_choice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(questionary_choice.errors.count, "error") %> prohibited this questionary_choice from being saved:</h2>

      <ul>
      <% questionary_choice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :選択肢の内容 %>
    <%= f.text_area :content, {cols:40, rows:5} %>
  </div>

  <div class="field">
    <%= f.label :選択肢の番号  %>

    <%
session[:questionary_count] ||= 0
session[:questionary_count] += 1
%>
<%= f.number_field :value, :value => session[:questionary_count] %>

  
   
  </div> 
 
  <div class="field">
   
    <%= f.hidden_field :questionary_item_id, :value => params[:id] %>
  </div>  

  <div class="actions">
    <%= f.submit (["選択肢の作成"])%>
  </div>
<% end %>
